import { AdminPlaceholder } from "@/components/admin/AdminPlaceholder";

export default function AdminShapesPage() {
  return (
    <AdminPlaceholder
      title="Shapes"
      phase="Phase 2–3"
      description="Manage Drum, Empire, Oval and other forms with images, compatible sizes and price modifiers."
    />
  );
}
